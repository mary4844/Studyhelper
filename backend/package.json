{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --runInBand --verbose",
    "test:tasks": "NODE_ENV=test jest --runInBand --verbose route_tests/tasks.unit.test.js",
    "test:calendar": "NODE_ENV=test jest --runInBand --verbose route_tests/calendar.unit.test.js",
    "test:boards": "NODE_ENV=test jest --runInBand --verbose route_tests/boards.unit.test.js",
    "test:routes": "NODE_ENV=test jest --runInBand --verbose route_tests/",
    "test:integration": "NODE_ENV=test jest --runInBand --verbose integration_tests/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-openid-connect": "^2.20.2",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "jest": "^30.3.0",
    "supertest": "^7.2.2"
  }
}